server {
    listen 5173;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback (HashRouter also works, but keep this for safety)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
