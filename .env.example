# ── Mission Control environment (copy to .env and fill in) ──────────────
# Never commit the real .env. This file is the public template.

# Database (local dev default; docker compose overrides with the db service)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mission_control

# API
PORT=3000
HOST=127.0.0.1
# CORS allowlist — comma-separated; both localhost spellings work via tunnel
WEB_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
# Shared secret for the event bridge (any strong random string)
INGEST_TOKEN=change-me

# Socket feed secret — guards the dashboard's Socket.IO connection when the api
# runs in production (NODE_ENV=production). Docker: compose injects this into the
# api container AND bakes it into the web bundle (VITE_SOCKET_TOKEN build arg);
# leave blank to use the compose default (dev-socket-token) on the loopback stack.
SOCKET_TOKEN=

# GitHub — enables PR approvals → auto-merge from Mission Control
GITHUB_TOKEN=

# Provider keys — enables LIVE balances in Health/Usage (DeepSeek only today)
DEEPSEEK_API_KEY=
ZAI_API_KEY=
